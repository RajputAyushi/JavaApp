pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                echo "passeed"
            }
        }
stage('Build'){
steps { 
  echo "build started"
   sh 'cd java-maven-sonar-argocd-helm-k8s/spring-boot-app && mvn clean package'
 echo "build successful"
    }


}
